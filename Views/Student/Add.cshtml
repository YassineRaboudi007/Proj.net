@model Gestion_note.Models.Matiere
@{
}
<div class="d-flex align-items-center justify-content-center my-3">
    <h3>Add Subject</h3>
</div>
<div class="container-sm ">
    <!-- Pills navs -->
    <!-- Pills content -->

    <div class="tab-pane fade show active d-flex align-items-center flex-column justify-content-center" id="pills-login" role="tabpanel" aria-labelledby="tab-login">
        <form class="d-flex flex-column align-items-center w-100" method="post">
            <!-- Email input -->
            <div class="form-outline mb-4 w-100">
                <label class="form-label" for="loginName">Name</label>
                <input type="text" id="loginName" class="form-control" asp-for="Name" />
            </div>

            <div class="form-outline mb-4 w-100">
                <label class="form-label" for="loginName">Coefficent</label>
                <input type="number" id="loginName" class="form-control" asp-for="Coefficent" />
            </div>
            @Html.DropDownList("CategoryId", (MultiSelectList)ViewBag.Categories, new { multiple = "multiple"})

            <!-- Submit button -->
            <button type="submit" class="btn btn-primary btn-block mb-4 w-100">Add Teacher</button>
        </form>
    </div>

